services:
  ku-gen-ai-dashboard:
    build: .
    container_name: ku_gen_ai_analytics
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
